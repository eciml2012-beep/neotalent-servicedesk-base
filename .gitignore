# Permisos y ajustes de Claude Code propios de cada máquina.
# El resto de .claude/ (skills) sí se versiona: es contexto del proyecto.
.claude/settings.local.json
.playwright-mcp/

# Herramientas de test (constitución, principio 6: la app no las necesita).
node_modules/
test-results/
playwright-report/

# Archivo local de Surge (recuerda el dominio de despliegue en esta máquina); cada persona
# publica al dominio de su propia sala, no hace falta compartirlo en el repo.
CNAME
